export type TBaseParams = {
  region: string;
  schoolName: string;
  teacherName: string;
  date: string;
  time: string;
  grade: number;
  subject: string;
};

export type TClassInfo = {
  teacherName: string;
  subject: string;
};
